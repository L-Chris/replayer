# 播放架构

## 模块与所有权

- `player/mod.rs`：异步 Player 句柄、命令、事件和状态快照。`open()` 不等待媒体初始化。
- `player/session.rs`：唯一的播放状态协调者。持有解码器和音频设备，优先补充音频，独立调度视频输出。视频输出使用非阻塞发送，过期帧可丢弃。
- `player/demux.rs`：独占 FFmpeg 输入上下文。通过有界消息通道传递原始 packet，处理定位；打开超时 30 秒，读取/定位超时 10 秒，Stop 可通过 FFmpeg interrupt callback 取消 I/O。
- `player/audio.rs`：解码、重采样及设备适配。预分配 0.5 秒的 SPSC PCM 环形缓冲。音频回调不加锁、不分配、不访问播放器状态互斥锁；只读控制原子量并发布播放进度。
- `player/clock.rs`：控制线程使用互斥锁维护完整时钟锚点；音频线程以单写者序列锁发布包含代次、媒体结束时间和设备预计播放结束时间的一致快照。短暂欠载时限制时钟不超过已提交音频的结束点；音频包持续缺失 250ms 后会话切换墙钟，防止短音轨与预读预算互相等待，音频恢复时裁掉迟到样本并重新接管时钟。
- `player/video.rs`：保留引用计数的 FFmpeg 原生帧，携带像素格式、色彩元数据和像素宽高比。Windows 尝试 D3D11VA，初始化不可用时回退软件解码；`REPLAYER_SOFTWARE=1` 可强制软件路径。
- `renderer.rs` / `video.wgsl`：复用平面纹理和输出纹理，以 GPU 将 8 位 YUV420P/YUVJ420P/NV12 转换为 RGB。其他格式走可复用的 swscale 缓冲。

## 生命周期

`Opening → Seeking（预缓冲）→ Playing / Paused → Draining → Ended`

任何读取/解码致命错误进入 `Failed`，真实原因通过 `Event::Error` 传给 UI。音频设备初始化失败或运行中失效通过 `Warning` 降级为无声视频。`Ended` 保留解封装线程和解码器，可以再次 Seek 或从头重播。

输入 EOF 后，两个解码器都发送 EOF 并读取剩余帧，重采样器也排空。音频环形缓冲为空还不代表播放结束：必须等待最后一个 sample 的设备播放时间。最终还需要等待视频最后一帧展示。

`Player` 析构立即禁用音频并设置取消标志，专用回收线程等待会话结束；会话回收解封装线程。UI 不同步 join 阻塞 I/O。

## Seek 协议

1. 调用方立即递增请求代次，旧视频帧从此不再返回，音频回调丢弃旧代次数据。
2. 会话合并已收到的 Seek，清除排队 packet / 帧，flush 解码器并重建重采样器。
3. 解封装线程完成定位后返回同代次 `Seeked`，后续 packet 均携带该代次。
4. 视频解码丢弃目标之前的帧，音频精确裁掉目标之前的 sample；音频时间戳空隙以有界、按需生成的静音填补。
5. 当前请求对应的视频输出准备好后等待音频预缓冲；已排队的音频包先解码到目标位置，如果没有音频包且已等待 100ms，则按音轨已结束或存在长空隙处理，先使用墙钟启动，之后音频到达再接管。发送 `SeekCompleted { id, position }`，恢复调用方要求的播放/暂停状态。暂停状态允许输出一张定位预览帧。

完成事件说明新位置已准备好，不表示设备已经播放到该位置。已提交给驱动的音频不能通过清空软件队列追回，交互延迟下限仍受设备延迟影响。

## 资源与性能边界

- 会话预读约 16 MiB 压缩 packet；单个 packet 不得超过 16 MiB。解封装通道另有 4 个消息槽，预算可能包含最后一个跨阈值 packet。
- 视频本地队列按当前分辨率采用保守 4 字节/像素预算，最多 8 帧；展示通道另有 3 帧，UI 最多保留 1 个待展示帧。FFmpeg 解码器内部参考帧和一次 packet 输出的额外帧不在此预算中。
- 所有队列均有上限。异常交错、超高码率输入仍受压缩包预算限制；不会为继续预读无限增大内存。
- 音视频解码目前由同一会话线程调度，慢的单次解码仍会延迟另一轨；这不是多解码 worker 架构。
- D3D11VA 硬解帧目前下载成原生 YUV，再交给 WGPU 上传和转色，**不是零拷贝**。跨 API 纹理共享、HDR tone mapping、旋转显示和中途音频格式变化仍需单独支持。
- GPU 快速路径处理常见 SDR 色彩矩阵和全/有限范围；不宣称完整 HDR 色彩管理。

## 验证

```powershell
cargo fmt --all -- --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked
cargo test --locked gpu_yuv_readback -- --ignored --nocapture
./verify.ps1
```

`--selftest <file>` 会断言异步打开、正常播放、展示消费暂停时的时钟进度、准确 Seek、暂停、暂停中 Seek、连续 Seek、EOF 排空、结束后 Seek 和重播。建议素材大于 2 秒。`verify.ps1` 使用项目自带 FFmpeg 生成可重复素材，覆盖无音轨、长 GOP/B 帧、音视频长短不同、44.1kHz 重采样、延迟音轨和 1080p/4K H.264；`verify.ps1 -Software` 强制软解复测。

GPU 测试默认忽略，显式运行时要求可用适配器；测试读取实际转换后的纹理，校验带 stride 的奇数尺寸 YUV420P/NV12 黑白及彩色样本。

`verify.ps1 -Stress` 额外生成约 165 MiB 的 4K 短音轨素材，检查音轨提前结束时，超过预读预算的视频是否仍能前进，以及定位到音轨结束之后能否完成。
