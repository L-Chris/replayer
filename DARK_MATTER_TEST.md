# Dark Matter 文件测试记录

输入：`data/Dark.Matter.2024.S02E01.mkv`。时长 3206.412 秒；2160×1080 HEVC 视频，48kHz、6 声道 E-AC-3 音频，另有内嵌 SubRip 字幕轨。

## 播放回归

该文件通过异步打开、正常播放、暂停、暂停中 Seek、连续 Seek、尾部排空、结束后 Seek 和重播断言。启用了 D3D11VA 硬件解码。此为自动抽样播放及定位测试，不表示实时播放并人工检查了全部 53 分钟。

## 实际模型请求

使用用户指定的 `https://chat.rethinkos.com/v1` 和 `qwen/omni-flash`，凭据仅从本地配置读取。

| 范围 | 目标语言 | 实际耗时 | 峰值并发 | 字幕条数 | 排版告警 |
| --- | --- | ---: | ---: | ---: | --- |
| 0–120 秒 | 简体中文 | 89.89 秒 | 3 | 14 | 阅读偏快 1，过短 0 |
| 600–660 秒 | English | 51.92 秒 | 3 | 9 | 阅读偏快 4，过短 2 |
| 20–40 秒，增强提示词复测 | 简体中文 | 39.85 秒 | 1（仅一个音频分段） | 5 | 阅读偏快 0，过短 0 |

前三组为不同时段/版本的功能测试，不是严格的串行与并行性能对照；没有据此宣称固定倍数加速。字幕生成未覆盖整集。最后一组使用了补足阅读时间和 `source/text` 输出约束。

样本文件：

- [片头中文](target/dark-matter-test/opening.zh-CN.srt)
- [中段英文](target/dark-matter-test/middle.en-US.srt)
- [提示词增强后的片头对白](target/dark-matter-test/opening-dialogue-v2.zh-CN.srt)

## 内容检查

从该文件的第一个内嵌字幕轨提取英文参考用于对照，没有将它替换成 AI 生成结果，也没有把参考文字提交给模型。

- 模型字幕大体对应相同对白时间，片头问句起始约 21.86 秒，内嵌参考约 22.02 秒；这不是逐字对齐。
- 初版漏掉了部分短句。加强约束后，20–40 秒复测补回了“Thanks.”，并保留了开头问句的问号。
- **识别误差仍存在**：约 31.8 秒的 “Help you?” 两次都被该模型误听并译成类似专名的文字。没有针对这句话硬编码修改结果。
- 排版告警只衡量长度、时长和阅读速度，不衡量语义准确率；成品字幕仍需校对。

## 自动化验证

普通测试 18 项通过（GPU 专项默认忽略），Clippy 无警告。新增测试包含语言配置序列化、带时间偏移的字幕、标点与小数/缩写、两行排版不丢字、引号内问号，以及三路请求同时到达、故意乱序响应后的顺序合并。网络测试使用本地服务，不消耗额度。
