# Replayer 字幕显示规范 v1

这是播放器的对白字幕默认样式，不是 Netflix 交付合规认证，也不等同于逐字强制对齐。

## 依据

- [Netflix 简体中文规范](https://partnerhelp.netflixstudios.com/hc/en-us/articles/215986007-Chinese-Simplified-Timed-Text-Style-Guide)：中文字幕的标点、每行长度和成人节目阅读速度。
- [Netflix 英文规范](https://partnerhelp.netflixstudios.com/hc/en-us/articles/217350977-English-USA-Timed-Text-Style-Guide)：英文断句、标点、每行长度和阅读速度。
- [Netflix 通用规范](https://partnerhelp.netflixstudios.com/hc/en-us/articles/215758617-Timed-Text-Style-Guide-General-Requirements)：行数、时长、居中和语义换行原则。

## 内容与标点

| 项目 | 简体中文 | English |
| --- | --- | --- |
| 每行目标上限 | 16 个全角字宽 | 42 个字符 |
| 最大行数 | 2 | 2 |
| 普通逗号、句号 | 用单个空格分隔分句，句末不留空格 | 保留正常标点 |
| 问号、感叹号 | 保留，统一为 `？`、`！`，去掉连续重复符号 | 保留正常英文形式 |
| 省略号 | 使用 `…`，不因跨条延续而补省略号 | 同左 |
| 阅读速度参考 | 9 字/秒 | 20 字符/秒 |

保留小数点、数字分隔符、缩写、枚举顿号与引号，不进行“一律删标点”。中文混排按显示字宽计数，半角字符约为半个全角字；这是 Replayer 的实现选择。引号内问句的问号放在闭引号之前。

同一请求要求模型返回原文转写 `source` 和目标语言 `text`。生成约束明确要求保留短句和问句，不按“是否重要”删掉寒暄或确认语。`source` 只帮助保留问句语气，不显示为第二语言；它仍由同一个模型识别，不能作为独立准确性验证。

## 换行与时间

- 优先单行；需要两行时选择接近平衡、下行略长的位置，优先标点/空格处，避免英文单词被拆开，也避开冠词与名词之间的换行。中文词法边界目前仍依赖模型和启发式规则。
- 超过两行容量的文本拆成连续字幕条，不截断文字。拆条时按文字长度分配原时间区间，因此这些细分时间也是近似值。
- 参考单条时长为 5/6 秒到 7 秒。尝试按阅读速度补足短条，但只在空闲间隙内最多延长 0.5 秒，不能覆盖下一条或跨越音频分段边界；异常长条最多显示 7 秒。
- 重叠条先合并再排版，输出按时间排序。无法同时满足同步和阅读速度时，保留对白并计入“阅读偏快/时长偏短”，不通过删词或大幅移动时间来掩盖问题。
- 上述规则针对对白字幕，不实现 SDH 音效描述、逐镜头切点对齐、歌词专用格式或复杂多人同屏排版。

## 视觉样式

白色无衬线文字，黑色描边；最多两行、逐行居中。字号随视频显示区域缩放，并根据实际字形宽度缩小以适应窄窗口。渲染器尊重排好的换行，不再自动折出第三行。

字幕位于视频下部安全区域，同时预留底部工具栏空间。工具栏淡入淡出时字幕位置保持稳定，不随透明度上下跳动。

生成结果与导出的 SRT 共用同一份规范化文本；外部播放器的字体和安全区仍由外部播放器控制。

## 自动验证与人工检查

单元测试覆盖中英文标点差异、小数与缩写、两行限制、长对白不丢字、无时间重叠、阅读速度告警、引号内问号。模型对口音、专名、背景声和短句仍会出错，排版合格不代表转写/翻译正确。
